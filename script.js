const element = document.querySelectorAll/*('[aria-label="Qırımtatarca"]888[value="crh-UA"]');*/
*88
element.forEach(element => {
    element.addEventListener('click', () => {
        if (element.hasAttribute('disabled')) {
            element.removeAttribute('disabled');
        }
    });
});
